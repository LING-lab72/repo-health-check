# Dark Theme

## Purpose

全局暗色主题 + CSS 动画过渡，提升视觉体验。

## Requirements

### Requirement: 暗色主题

系统 SHALL 使用 CSS 变量实现全局暗色主题。

### Requirement: 动画过渡

系统 SHALL 为卡片 hover、页面进入添加 CSS transition 和 keyframes 动画。

### Requirement: 响应式布局

系统 SHALL 支持移动端适配（< 768px）。
